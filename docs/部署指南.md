# 部署指南

## 服务器信息

| 项目 | 值 |
|------|----|
| 服务器 IP | 47.236.94.252 |
| 登录方式 | ssh root@47.236.94.252 |
| 项目目录 | /root/workspace/btcquant |
| 代码仓库 | https://github.com/ningersweet/btcquant.git |

## 服务器配置

| 资源 | 总量 | 系统可用 |
|------|------|----------|
| CPU | 2 核 | 1 核 |
| 内存 | 4 GB | 2 GB |
| 磁盘 | 100 GB | - |

## 资源分配

每个服务的资源限制：

| 服务 | CPU 限制 | 内存限制 |
|------|----------|----------|
| data-service | 0.25 核 | 512 MB |
| features-service | 0.25 核 | 512 MB |
| predict-service | 0.25 核 | 512 MB |
| strategy-service | 0.25 核 | 512 MB |
| **总计** | 1 核 | 2 GB |

## 快速部署

### 1. 登录服务器

```bash
ssh root@47.236.94.252
```

### 2. 执行部署脚本

```bash
# 首次部署
mkdir -p /root/workspace
cd /root/workspace
git clone https://github.com/ningersweet/btcquant.git
cd btcquant
chmod +x scripts/deploy.sh
./scripts/deploy.sh
```

### 3. 配置环境变量

```bash
# 编辑 .env 文件
vim .env

# 添加 Binance API 密钥
BINANCE_API_KEY=your_api_key
BINANCE_API_SECRET=your_api_secret
```

### 4. 重启服务

```bash
docker-compose restart
```

## 常用命令

```bash
# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f data-service
docker-compose logs -f --tail=100

# 重启服务
docker-compose restart

# 停止服务
docker-compose down

# 更新代码并重新部署
git pull
docker-compose build
docker-compose up -d

# 查看资源使用
docker stats
```

## 日志管理

日志配置：
- 日志驱动: json-file
- 单文件大小: 10MB
- 保留文件数: 3

查看日志位置：
```bash
docker inspect btc_quant_data_service | grep LogPath
```

## 健康检查

```bash
# 检查各服务健康状态
curl http://localhost:8001/health
curl http://localhost:8002/health
curl http://localhost:8003/health
curl http://localhost:8004/health
```

## 故障排查

### 服务无法启动

```bash
# 查看详细日志
docker-compose logs --tail=50 data-service

# 检查端口占用
netstat -tlnp | grep 800
```

### 内存不足

```bash
# 查看内存使用
free -h

# 清理 Docker 缓存
docker system prune -a
```

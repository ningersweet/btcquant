# 配置管理快速参考

## 🎯 核心原则

```
本地环境（主配置） → 服务器（副本）
```

## ✅ 正确流程

```bash
# 1. 本地修改
vim config.yaml

# 2. 同步到GPU服务器
btcquant train sync-config

# 3. 同步到CPU服务器（如需要）
scp config.yaml cpu_server:/root/workspace/btcquant/
```

## ❌ 禁止操作

- ❌ 直接在服务器上修改 config.yaml
- ❌ 从服务器复制 config.yaml 到本地
- ❌ 提交 config.yaml 到 Git
- ❌ 修改后不同步就启动训练

## 📋 常用命令

```bash
# 同步配置到GPU服务器
btcquant train sync-config

# 同步配置到CPU服务器
scp config.yaml cpu_server:/root/workspace/btcquant/

# 检查配置差异
diff config.yaml <(ssh gpu_server "cat ~/workspace/btcquant/config.yaml")

# 备份配置
cp config.yaml config.yaml.backup.$(date +%Y%m%d)
```

## 🔄 修改场景

### 训练参数
```bash
vim config.yaml  # 修改 predict.training.*
btcquant train sync-config
btcquant train start --gpu
```

### 邮件配置
```bash
vim config.yaml  # 修改 notification.email.*
python predict/scripts/test_email.py  # 测试
btcquant train sync-config
```

### 标签参数
```bash
vim config.yaml  # 修改 predict.label.*
scp config.yaml cpu_server:/root/workspace/btcquant/
btcquant data prepare  # 重新生成标签
btcquant train sync-data
```

---

**记住：本地是唯一真实来源！** 🎯
